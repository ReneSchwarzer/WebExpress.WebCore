namespace WebExpress.WebCore.WebDomain
{
    /// <summary>
    /// Represents a logical application domain used to categorize or group
    /// components, pages or functional areas within the system. Implementations
    /// define domain-specific metadata or identifiers that can be used for
    /// routing, filtering or contextual association.
    /// </summary>
    public interface IDomain
    {
    }
}
