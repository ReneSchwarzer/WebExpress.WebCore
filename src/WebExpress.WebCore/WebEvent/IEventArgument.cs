namespace WebExpress.WebCore.WebEvent
{
    /// <summary>
    /// Marks a type as the data payload passed to event handlers when an event is raised. It carries
    /// whatever information a handler needs to react to that particular event.
    /// </summary>
    public interface IEventArgument
    {
    }
}