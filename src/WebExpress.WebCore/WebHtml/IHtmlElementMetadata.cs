namespace WebExpress.WebCore.WebHtml
{
    /// <summary>
    /// Marker interface that tags an HTML element as belonging to the HTML5 "metadata content"
    /// category — elements that describe the document rather than show content, such as those that
    /// live in the page head. It carries no members; it only lets the code group elements by this category.
    /// </summary>
    public interface IHtmlElementMetadata
    {
    }
}