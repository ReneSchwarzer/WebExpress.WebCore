namespace WebExpress.WebCore.WebHtml
{
    /// <summary>
    /// Marker interface that tags an HTML element as belonging to the HTML5 "sectioning content"
    /// category — elements that define a section of the page outline, such as <c>&lt;section&gt;</c>,
    /// <c>&lt;article&gt;</c>, or <c>&lt;nav&gt;</c>. It carries no members; it only lets the code group elements by this category.
    /// </summary>
    public interface IHtmlElementSection
    {
    }
}