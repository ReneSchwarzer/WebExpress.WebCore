namespace WebExpress.WebCore.WebHtml
{
    /// <summary>
    /// Marker interface that tags an HTML element as belonging to the HTML5 "interactive content"
    /// category — elements meant for user interaction, such as buttons, links, or form fields.
    /// It carries no members; it only lets the code group elements by this category.
    /// </summary>
    public interface IHtmlElementInteractive
    {
    }
}