namespace WebExpress.WebCore.WebHtml
{
    /// <summary>
    /// Marker interface that tags an HTML element as a document root element (the <c>&lt;html&gt;</c>
    /// element that wraps the whole page). It carries no members; it only lets the code group
    /// elements by this category.
    /// </summary>
    public interface IHtmlElementRoot
    {
    }
}