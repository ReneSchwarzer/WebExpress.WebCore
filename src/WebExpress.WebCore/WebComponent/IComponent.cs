namespace WebExpress.WebCore.WebComponent
{
    /// <summary>
    /// Common base type for the building blocks a plugin contributes to WebExpress — applications,
    /// pages, resources, REST APIs, event handlers, identity policies, and so on. The framework
    /// discovers component types, creates them, and manages their lifetime; this interface is the
    /// shared marker that lets them be handled uniformly.
    /// </summary>
    public interface IComponent
    {

    }
}
