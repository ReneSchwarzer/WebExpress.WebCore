namespace WebExpress.WebCore.WebComponent
{
    /// <summary>
    /// Contract for a component identifier — the value that uniquely names a component so it can be
    /// referenced and looked up. See <see cref="ComponentId"/> for the standard implementation.
    /// </summary>
    public interface IComponentId
    {
    }
}
