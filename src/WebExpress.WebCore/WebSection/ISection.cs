namespace WebExpress.WebCore.WebSection
{
    /// <summary>
    /// Marker type that names a "section" — a placeholder location in a page or layout into which
    /// fragments can be inserted. Fragments declare the section they belong to, and the framework
    /// renders them at that spot.
    /// </summary>
    public interface ISection
    {
    }
}
