namespace WebExpress.WebCore.Test.WebSocket
{
    public class UnitTestWebSocketConnection
    {
        [Fact]
        public async Task ClientConnection()
        {
            //using var client = new ClientWebSocket();

            //await client.ConnectAsync(new Uri("ws://localhost:5000/socket"), CancellationToken.None);
        }
    }
}
